rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Cho phép đọc và ghi dữ liệu phục vụ ứng dụng Kho Kiểm Tra AI
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
